gpt2_run
*.o
__pycache__/
*.pyc
downloads/

# Base model weights — too large for git, tracked via LFS instead
# weights/model.bin  (tracked via LFS)
# weights/emily-ft.bin  (tracked via LFS)

# Intermediate training checkpoints (not the final fine-tune)
checkpoint-*/
!checkpoint-emily-ft/

# Training run logs
var/train-*.log

# Regenerable corpus builds (rebuild via emily train build-dataset)
var/emily-corpus.jsonl
var/snapshots/
weights/emily-ft-20260623.bin
